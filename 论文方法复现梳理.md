# Soltan et al. (2026) TD3-RCMPSP 方法复现梳理

> 论文：*A novel deep reinforcement learning method for solving resource-constrained multi-project scheduling problem: twin delayed deep deterministic policy gradient*。
>
> 本文档以可实施复现为目的。数据集已在 `MPSPLIB/RCMP/`，重点说明论文方法、输入输出接口和原文未交代而必须自行确定的设计。

## 1. 一句话概括

把经典的资源约束多项目调度问题（RCMPSP）写成一个连续动作 MDP：智能体在每个调度决策时刻，为当前满足前序约束的活动分配共享可再生资源；用 TD3 学习使总完工期（makespan）最小的策略。

论文的实质创新是将 TD3 用于该问题；TD3 相对 DDPG 采用双 Critic 取最小值、目标策略平滑和延迟 Actor 更新，以减小价值高估和训练不稳定。

## 2. 问题范围与目标

论文处理的是经典、确定性的单模式 RCMPSP：

| 项目 | 论文假设 |
| --- | --- |
| 活动关系 | Finish-to-Start 前序约束 |
| 资源 | 全局共享、可再生、不可替代 |
| 执行方式 | 单模式、不可抢占 |
| 不确定性 | 工期、资源容量和需求均确定 |
| 优化目标 | 最小化全部项目的总完工期 `Cmax` |

对每个活动 `i`，需要满足前序约束 `S_j + d_j <= S_i`；对每个资源 `r` 和时刻 `t`，并行活动的需求之和不超过容量 `R_r`。最终指标为所有项目完成时间的最大值。

## 3. 论文数据集与本地数据的对应

论文使用 MPSPLIB（Homberger, 2007），称包含 140 个实例，规模类别为 `j30/j60/j90/j120`。本地 `MPSPLIB/RCMP/` 恰有 140 个 `.rcmp` 文件：`j30` 20 个、`j90` 60 个、`j120` 60 个；没有 `j60` 文件。文件名中的 `j30/j90/j120` 是**单个项目**的活动数，`a2/a5/a10/a20` 是项目数，`nr1` 等是实例编号。

论文主实验称选择了一个 7 项目、每项目 92 个活动（含两个 dummy）和 4 个全局可再生资源的实例，但**没有给出该实例文件名**。因此主实验表 5--7 的数值（TD3 makespan 6192）目前不能和本地具体文件一一对齐。

论文灵敏度分析明确使用下列本地实例：

| 论文实例 | 本地路径 | 项目数 | 总活动数（含 dummy） | 全局资源 | OLF |
| --- | --- | ---: | ---: | ---: | --- |
| 1 | `MPSPLIB/RCMP/mp_j30_a2_nr1.rcmp` | 2 | 64 | 4 | High |
| 2 | `MPSPLIB/RCMP/mp_j30_a5_nr1.rcmp` | 5 | 160 | 4 | Low |
| 3 | `MPSPLIB/RCMP/mp_j30_a5_nr3.rcmp` | 5 | 160 | 4 | High |

### `.rcmp` 文件需要解析出的内容

文件开头给出项目数、资源数、每种全局资源容量。随后每个项目有一个活动块：每行依次是该活动对各资源的需求、与活动有关的附加字段、后继数量、以及 `项目号:活动号` 形式的后继列表。每个项目都有工期为 0 的开始/结束 dummy 活动。实现前应先为这三个表建立结构化对象：

```text
Project(id)
  Activity(id, duration, demand[R], predecessors, successors, is_dummy)
Instance(projects, capacities[R])
```

注意：本地文件的列语义不能只靠 README 推断；应以 MPSPLIB 的 RCMP 格式说明或通过活动块首尾行（dummy 需求/工期均为 0）交叉验证。论文没有重新定义该格式。

## 4. MDP 设计（论文给出的定义）

| 组件 | 论文定义 | 实现含义 |
| --- | --- | --- |
| Agent | 项目组合经理 | 一个集中式 TD3 智能体 |
| State | 全部活动进度、前序可行标志、资源余量、活动工期/剩余资源需求、已流逝时间 | 固定长度数值向量，资源值归一化 |
| Action | 选择可行活动，并决定向各活动分配多少资源 | 连续资源分配向量 |
| Transition | 活动进展、资源可用量变化、前序约束更新 | 调度环境的 `step()` |
| Reward | 与总完工时间成比例的负奖励 | 目标是最小化 makespan |
| Terminal | 所有活动完成 | episode 结束，报告 `Cmax` |

论文将完整排程表示为轨迹 `[(s_t, a_t)]_(t=1)^T`，而不是活动列表或优先级排列。Actor 给出 `a_t = mu_theta(s_t)`；训练时增加裁剪探索噪声。所谓“邻域”来自连续动作的小扰动、目标策略平滑和 replay buffer 中的多条轨迹，而非交换/插入等显式邻域算子。

## 5. TD3 算法

给定回放样本 `(s, a, r, s_next, done)`：

```text
a_target = clip(actor_target(s_next) + clip(N(0, sigma), -c, c), action_low, action_high)
y = r + gamma * (1 - done) * min(critic1_target(s_next, a_target), critic2_target(s_next, a_target))
critic_i <- minimize MSE(critic_i(s, a), y), i in {1, 2}
每 2 次 critic 更新：
  actor <- maximize critic1(s, actor(s))
  actor_target, critic_target <- Polyak soft update
```

这三个 TD3 机制必须保留：

- 双 Critic，目标值取二者较小值。
- 目标 Actor 输出加裁剪噪声（target policy smoothing）。
- Actor 和 target network 每 2 次 Critic 更新一次（delayed update）。

## 6. 论文超参数与训练方案

| 项目 | 值 |
| --- | --- |
| Actor MLP | `state -> 256 -> 256 -> 128 -> action_dim` |
| Critic MLP | `(state, action) -> 256 -> 256 -> 128 -> 1` |
| Actor / Critic 学习率 | `3e-4 / 3e-4` |
| 优化器 | Adam |
| 折扣因子 `gamma` | 0.99 |
| batch size | 100 |
| target soft update `tau` | `5e-3` |
| exploration noise | 0.1 |
| policy update frequency | 2 |
| policy clip | 0.5 |
| replay buffer capacity | `1e6` |
| training round | `1e6`（表述有歧义，见下文） |

调参使用 Optuna/TPE：50 个 trial，每个 trial 从头训练 1400 episodes；优化 Actor LR、Critic LR、batch size、exploration noise；在固定的 5 个验证实例上最小化最终策略的平均 makespan。搜索范围分别为 `[1e-5, 1e-3]`、`[1e-5, 1e-3]`、`[32, 256]`、`[0.01, 0.3]`。

## 7. 必须补齐的复现决策

这部分决定能否训练出一个有效系统。以下细节在论文中没有给出，不能伪称为论文的原始实现。

| 缺失项 | 原文只说明 | 建议的首版可复现实现在文档中固定为 |
| --- | --- | --- |
| 动作维度 | “为可行活动分配连续资源” | 使用每个活动一个连续 priority/logit；环境以 mask 过滤前序可行的未开始活动，再按分数贪心启动，形成合法的离散排程。直接输出“资源量”会和不可抢占、固定资源需求冲突。 |
| 动作可行化 | 无 | Actor 输出 `[-1, 1]^N`，仅对 eligible 活动排序；SSGS 在最早资源可行时刻启动活动。动作只决定顺序，环境保证可行性。 |
| 时间推进 | 无 | event-driven：启动全部当前可启动活动后，跳到最早完成事件，释放资源、完成活动、更新 eligible 集合。 |
| 奖励公式 | “与总完成时间成比例的负奖励” | 每步 `r_t = -(t_next - t)`，终局不再额外奖励；累计 undiscounted reward 恰为 `-Cmax`。若使用 `gamma=0.99`，训练目标是折扣后的近似，评估仍只用 `Cmax`。 |
| 状态固定维度 | “全部活动状态和需求” | 设定最大活动数 `N_max`，padding + activity mask；或先按同尺寸实例训练，避免 MLP 不能跨维度泛化。 |
| 跨实例训练/划分 | 仅称训练交互动态产生，验证用 5 个实例 | 明确写出 train/validation/test 文件清单，保证同一 `.rcmp` 不同时出现于三者。 |
| target noise std | 公式有 `sigma`，表中未列 | 先采用 TD3 常用值 0.2，clip 0.5；日志中标识为“复现假设”。 |
| 激活、初始化、随机种子、warm-up | 未给出 | ReLU、Actor 输出 `tanh`、5 个种子、先随机交互填充 replay buffer 至 batch size。 |

其中最重要的建模判断是：标准 RCMPSP 中活动一旦启动就占用其固定资源需求直至结束，资源并非可任意连续切分。论文同时声称“连续资源分配”和“不可抢占单模式活动”，但未说明转换机制。若直接将 action 解释为资源份额，必须另外定义份额不足时活动是否允许部分执行；这会改变原问题。因此首版建议用**连续 priority 动作 + 可行 SSGS 解码器**，把 TD3 的连续控制与合法的经典排程衔接起来。

## 8. 实施顺序

1. 写 RCMP 解析器，并用 `mp_j30_a2_nr1.rcmp` 做单元检查：活动总数 64、资源数 4、每条后继边可反查为前序边。
2. 写确定性的 event-driven SSGS，不接 RL 时先用 FIFO、最短工期、随机 priority 生成可行排程；验证资源和前序约束永不违反。
3. 将 SSGS 包成 Gymnasium 环境，完成 `reset/step/observation/action_mask` 和 `Cmax` 评估。
4. 实现 TD3、replay buffer、训练/验证循环；先在单一小实例过拟合，确认 reward 降低和排程合法。
5. 加入多实例采样、固定验证集、Optuna 调参和 DDPG 消融基线。
6. 在论文明确的三个灵敏度实例上比较 TD3/DDPG，并报告每个 seed 的 makespan、均值和标准差，而非只报告最优 run。

## 9. 与论文结果的对比边界

论文报告主实验：TD3 makespan 6192、DDPG 9900、rollout metaheuristic 8478；论文还称 TD3 推理每个排程 15--25 秒。但由于主实验实例名、环境转移和动作解码均未公开，无法把该数值当作严格可复现的验收线。

更可靠的验收标准依次是：

1. 每个输出 schedule 满足所有前序和资源约束；
2. 同一实例上 TD3 显著优于随机 priority 和简单优先规则；
3. 在相同环境、预算和随机种子下，TD3 的均值不差于 DDPG；
4. 在三个明确实例上复现论文所述的 TD3 优势趋势，特别观察高 OLF 情形。

## 10. 论文定位

原文的模型、MDP 和 TD3 介绍位于第 3 节与第 6 节（文中小节编号从 3.5 后跳为 6.1，属于论文排版问题）；数据集和调参位于第 7.1--7.2 节；灵敏度实例在表 8。论文没有公开代码，且 Data availability 写为“available on request”。
